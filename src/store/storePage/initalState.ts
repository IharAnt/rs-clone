import { typeStorePage } from "../types/types";

const initalState: typeStorePage = {
    basketProducts: [],
    orders: [],
    basketCount: 0,
    IsBasketOpen: false,
    IsHistoryOpen: false,
}

export default initalState;
